//TEMPLATE STRING - hace referencia a las comillas invertidas -> ``
const division = (num1, num2) =>
  `La division de ${num1} y ${num2} es ${num1 / num2}`;
const resultadoDiv = division(18, 3);
console.log(resultadoDiv);
